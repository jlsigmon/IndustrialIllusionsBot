module.exports = {
    name: 'restart',
    description: "Gives users info on different commands",
    execute(msg){
        let restart;
        if(msg.author.id === 'your-user-id-here'){
            if(restart.includes('lol')){
                msg.reply('yes');
            }
        }
    }
}